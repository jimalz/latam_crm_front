export interface SignupPayload {
  name: string;
  email: string;
  password: string;

  // future fields:
  // phone?: string;
  // company?: string;
  // country?: string;
  // timezone?: string;
  // avatar?: string;
}
